<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!--
    Pending timers hold a partial wake lock so the CPU keeps counting while the
    screen is off. See BackgroundTimerModule.
  -->
  <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>
